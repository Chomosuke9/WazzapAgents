httpx>=0.27.0
pydantic>=2.7.0
websockets>=12.0
langchain>=0.2.0
langchain-openai>=0.1.0
python-dotenv>=1.0.1
Pillow>=10.0.0
# Used by python/bridge/subagent/webhook_server.py to receive
# complete/progress callbacks from WazzapSubAgents. Without it the bridge
# silently falls back to 120 s polling for every sub-agent task.
aiohttp>=3.9.0
